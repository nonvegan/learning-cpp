/*
int decNumber = 21;
int octalNumber = 025;
int hexNumber = 0x15;
cout << "DecNumber: " << decNumber << ", ocatalNumber: " << octalNumber << ", hexNumber: " << hexNumber << endl;
*/


//float x = 5 / 2;
//float y = 5.0 / 2;
//double z = (float)5 / 2;
//cout << x << " " << y << " " << z << endl;


	/*enum class Season { spring, summer, fall, winter };
	Season current = Season::fall;

	switch (current) {
	case Season::spring:
		cout << "It's Spring!" << endl;
		break;
	case Season::summer:
		cout << "It's Summer!" << endl;
		break;
	case Season::fall:
		cout << "It's Fall!" << endl;
		break;
	case Season::winter:
		cout << "It's winter!" << endl;
		break;
	default:
		cout << "I don't understand!" << endl;
		break;
	}*/

	//string sentence = "Hello, World!";
	//for (int i = 0; i < sentence.length(); i++) {
	//	cout << sentence[i] << "|";
	//}

/*int input;
	cin >> input;
	string output = input == 0 ? "Input igual a zero" : "Input diferente de zero";
	cout << output << endl;*/

	/*int numbers[10];
		for (int i = 0; i < 5; i++) {
			cin >> numbers[i];
		}
		for (int i = 0; i < 5; i++) {
			cout << numbers[i];
			if (i < 9) {
				cout << ", ";
			}
		}

		cout <<  UINT64_MAX;*/
